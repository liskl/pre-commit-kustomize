-   id: kustomize
    name: kustomize (via docker)
    description: kustomize
    entry: kustomize build --enable-alpha-plugins
    language: docker
    args: [deploy/kustomize/overlay/dev]
    always_run: true
    pass_filenames: false
